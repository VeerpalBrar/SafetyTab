function add_site(){
  var site_url = $('#site_name').val();
  if(!check_url_valid(site_url)){
    sire_url = fix_url()
  }
  //message background js to number of sites?
  // add site and then update number of sites
}
